Tests

Run tests locally:

- Install dependencies: `npm install --legacy-peer-deps`
- Run tests in watch mode: `npm run test`
- Run tests once (CI style): `npm run test:ci`

CI:

A GitHub Actions workflow (`.github/workflows/ci.yml`) runs the test suite on pushes and pull requests.